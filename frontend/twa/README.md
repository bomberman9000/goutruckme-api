# GruzPotok Telegram WebApp (TWA)

Minimal frontend scaffold for in-bot cabinet.

## Run

```bash
cd frontend/twa
npm install
npm run dev
```

## Build

```bash
npm run build
```

The app reads feed from `GET /api/v1/feed`.
